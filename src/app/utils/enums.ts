export enum SortAlgoValues {
    BubbleSort = 1,
    QuickSort = 2,
    SelectionSort = 3,
    InsertionSort = 4
}

export enum ColorConsts {
    Selected = 'selected',
    Sorted = 'sorted',
    Traversing = 'traverse'
}